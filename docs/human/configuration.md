# Configuration Reference

Aionima is configured via two files: `gateway.json` (structure and behavior) and `.env` (secrets). This document covers every available configuration field, how `.env` integration works, and how hot-reload operates.

---

## File Locations

| File | Location | Purpose |
|------|---------|---------|
| `gateway.json` | `~/.agi/gateway.json` | All configuration |
| `.env` | `~/.agi/.env` | Secrets (tokens, API keys) |

The gateway looks for `gateway.json` in the current working directory by default. Override with `--config <path>` on the CLI.

> **Renamed from `aionima.json`.** On first boot after upgrade, any existing `~/.agi/aionima.json` is auto-renamed to `~/.agi/gateway.json` in place (see `cli/src/config-loader.ts`). No user action required. The security scanner (`CFG-DEBUG-MODE`) still scans both filenames for one release so older dev checkouts continue to audit cleanly.

---

## Secrets Interpolation

Any string value in `gateway.json` can reference an environment variable using `$ENV{VARIABLE_NAME}` syntax:

```json
{
  "auth": {
    "tokens": ["$ENV{AUTH_TOKEN}"]
  }
}
```

At load time, `$ENV{AUTH_TOKEN}` is replaced with the value of the `AUTH_TOKEN` environment variable. Variables are read from the process environment, which includes values loaded from `.env` at startup.

Never put raw API keys or tokens directly in `gateway.json`. The `aionima doctor` command checks for common secret patterns and warns if it finds them.

---

## .env File

The `.env` file is loaded by the gateway at startup using a simple `KEY=VALUE` parser. It must have `0600` permissions (readable only by the owner).

```bash
# Aionima environment — secrets (mode 0600)
# Generated by `aionima setup`

AUTH_TOKEN=64-char-random-hex-string
ANTHROPIC_API_KEY=sk-ant-api03-...
TELEGRAM_BOT_TOKEN=123456789:AAF...
DISCORD_BOT_TOKEN=MTIzNDU2...
GMAIL_CLIENT_ID=123456789-xxxx.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=GOCSPX-...
GMAIL_REFRESH_TOKEN=1//...
SIGNAL_API_URL=http://localhost:8080
WHATSAPP_ACCESS_TOKEN=EAA...
WHATSAPP_PHONE_NUMBER_ID=123456789012345
JWT_SECRET=64-char-random-hex-string
```

Set permissions:

```bash
chmod 0600 /opt/agi/.env
```

---

## Full Configuration Schema

### gateway

Controls the HTTP server binding and release channel.

```json
{
  "gateway": {
    "host": "127.0.0.1",
    "port": 3100,
    "updateChannel": "dev"
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `host` | string | `"127.0.0.1"` | Listen address. Use `"0.0.0.0"` to expose on all interfaces. |
| `port` | number | `3100` | HTTP port |
| `updateChannel` | `"main" \| "dev"` | `"main"` | Which branch `agi upgrade` pulls from |

> **Operational state is not a config field.** `ONLINE` / `LIMBO` / `OFFLINE` / `UNKNOWN` are computed at runtime from AGI's connection to Aionima-prime + Hive-ID and exposed read-only at `GET /api/gateway/state`. See the [state machine docs](../agents/state-machine.md).
>
> A `gateway.state` value in `gateway.json` from an earlier version is accepted and ignored. Remove it next time you edit the file.

### auth

Authentication and rate limiting for the gateway API.

```json
{
  "auth": {
    "tokens": ["$ENV{AUTH_TOKEN}"],
    "maxAttemptsPerWindow": 10,
    "rateLimitWindowMs": 60000,
    "lockoutDurationMs": 300000,
    "maxBodyBytes": 2097152
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `tokens` | string[] | `[]` | Bearer tokens that grant access |
| `password` | string | — | Optional password-based auth |
| `maxAttemptsPerWindow` | number | `10` | Max failed auth attempts per IP before lockout |
| `rateLimitWindowMs` | number | `60000` | Auth rate limit window (ms) |
| `lockoutDurationMs` | number | `300000` | Lockout duration after too many failures (ms) |
| `maxBodyBytes` | number | `2097152` | Max request body size (2 MB) |

Loopback addresses (`127.0.0.1`, `::1`) bypass auth checks when no token is configured.

### agent

LLM provider configuration.

```json
{
  "agent": {
    "provider": "anthropic",
    "model": "claude-sonnet-4-6",
    "maxTokens": 8192,
    "maxRetries": 3,
    "replyMode": "autonomous",
    "devMode": false
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `provider` | enum | `"anthropic"` | LLM provider: `anthropic`, `openai`, `ollama` |
| `model` | string | `"claude-sonnet-4-6"` | Model identifier |
| `maxTokens` | number | `8192` | Max response tokens |
| `maxRetries` | number | `3` | Retry attempts on transient errors |
| `baseUrl` | string | — | Base URL for Ollama or proxy |
| `replyMode` | enum | `"autonomous"` | `autonomous` or `human-in-loop` |
| `devMode` | boolean | `false` | Enable developer identity and workspace context |
| `providers` | array | — | Failover provider list |
| `resourceId` | string | `"$A0"` | COA resource identifier |
| `nodeId` | string | `"@A0"` | COA node identifier |

### channels

Array of channel configurations.

```json
{
  "channels": [
    {
      "id": "telegram",
      "enabled": true,
      "config": {
        "botToken": "$ENV{TELEGRAM_BOT_TOKEN}"
      }
    }
  ]
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `id` | string | required | Channel plugin ID |
| `enabled` | boolean | `true` | Whether this channel is active |
| `config` | object | — | Channel-specific configuration (varies by channel) |

### owner

Owner identity and access configuration.

```json
{
  "owner": {
    "displayName": "Owner",
    "dmPolicy": "pairing",
    "channels": {
      "telegram": "123456789",
      "discord": "987654321098765432",
      "gmail": "you@example.com",
      "signal": "+15555550100",
      "whatsapp": "+15555550100"
    }
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `displayName` | string | `"Owner"` | Display name injected into the system prompt |
| `dmPolicy` | enum | `"pairing"` | `pairing` (code required) or `open` (allow all) |
| `channels` | object | `{}` | Per-channel owner user IDs |

### entities

SQLite entity store configuration.

```json
{
  "entities": {
    "path": "./data/entities.db"
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `path` | string | `"./data/entities.db"` | Path to the SQLite database file |

> **Note:** The schema default shown above is `./data/entities.db`. In production, the runtime config places the database at `~/.agi/entities.db`. The `aionima setup` wizard writes the production path automatically.

### queue

Message queue processing.

```json
{
  "queue": {
    "pollIntervalMs": 100,
    "concurrency": 10,
    "drainTimeoutMs": 5000
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `pollIntervalMs` | number | `100` | Queue poll interval |
| `concurrency` | number | `10` | Max concurrent message processing |
| `drainTimeoutMs` | number | `5000` | Shutdown drain timeout |

### sessions

Agent session management.

```json
{
  "sessions": {
    "contextWindowTokens": 200000,
    "idleTimeoutMs": 86400000,
    "maxSessions": 5000
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `contextWindowTokens` | number | `200000` | Token budget per session |
| `idleTimeoutMs` | number | `86400000` | Session idle timeout (24 hours) |
| `maxSessions` | number | `5000` | Max concurrent sessions |

### skills

Skills system configuration.

```json
{
  "skills": {
    "directory": "./skills",
    "watchForChanges": false
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `directory` | string | `"./skills"` | Directory containing `.skill.md` files |
| `watchForChanges` | boolean | `false` | Hot-reload skills on file changes |

### memory

Composite memory adapter configuration.

```json
{
  "memory": {
    "directory": "./data/memory"
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `directory` | string | `"./data/memory"` | Directory for file-based memory storage |

### workspace

Developer workspace configuration.

```json
{
  "workspace": {
    "root": ".",
    "projects": ["/home/user/projects"],
    "selfRepo": "/home/user/projects/aionima",
    "webhookSecret": "$ENV{WEBHOOK_SECRET}"
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `root` | string | `"."` | Workspace root for file tools |
| `projects` | string[] | `[]` | Directories where projects are hosted |
| `selfRepo` | string | — | Path to this repo (enables dashboard update detection) |
| `webhookSecret` | string | — | GitHub webhook HMAC secret |

### voice

Voice pipeline configuration.

```json
{
  "voice": {
    "enabled": false,
    "sttProvider": "whisper",
    "ttsProvider": "edge",
    "whisperModel": "whisper-1"
  }
}
```

See the [Voice Pipeline](./voice.md) document for full field details.

### hosting

Project hosting configuration.

```json
{
  "hosting": {
    "enabled": false,
    "lanIp": "192.168.0.144",
    "baseDomain": "ai.on",
    "portRangeStart": 4000,
    "containerRuntime": "podman"
  }
}
```

See the [Project Hosting](./project-hosting.md) document for full field details.

### logging

Log output configuration.

```json
{
  "logging": {
    "level": "info",
    "logDir": "./logs",
    "maxFileSize": 10485760,
    "maxFiles": 5,
    "stdout": true
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `level` | enum | `"info"` | Log level: `debug`, `info`, `warn`, `error` |
| `logDir` | string | `"./logs"` | Log file directory |
| `maxFileSize` | number | `10485760` | Max log file size (10 MB) before rotation |
| `maxFiles` | number | `5` | Max rotated log files to keep |
| `stdout` | boolean | `true` | Also write logs to stdout |

### heartbeat

Autonomous heartbeat configuration.

```json
{
  "heartbeat": {
    "enabled": false,
    "intervalMs": 3600000,
    "promptPath": "./data/persona/HEARTBEAT.md"
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `enabled` | boolean | `false` | Enable autonomous heartbeat |
| `intervalMs` | number | `3600000` | Interval between heartbeats (1 hour) |
| `promptPath` | string | `"./data/persona/HEARTBEAT.md"` | Path to heartbeat prompt file |

### dashboardAuth

Multi-user dashboard authentication.

```json
{
  "dashboardAuth": {
    "enabled": false,
    "jwtSecret": "$ENV{JWT_SECRET}",
    "sessionTtlMs": 86400000
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `enabled` | boolean | `false` | Enable multi-user auth |
| `jwtSecret` | string | — | Secret for signing session tokens |
| `sessionTtlMs` | number | `86400000` | Session TTL (24 hours) |

### prime

PRIME knowledge corpus location. Defaults to the system-level repo at `/opt/agi-prime`.

```json
{
  "prime": {
    "dir": "/opt/agi-prime",
    "source": "git@github.com:Civicognita/aionima.git",
    "branch": "main"
  }
}
```

### workers

Taskmaster engine settings. Workers are registered by plugins and discovered at boot.

```json
{
  "workers": {
    "autoApprove": false,
    "maxConcurrentJobs": 3,
    "workerTimeoutMs": 300000,
    "modelOverrides": {
      "code.hacker": {
        "provider": "anthropic",
        "model": "claude-opus-4-6"
      }
    }
  }
}
```

### federation

Federation protocol configuration. Enables this node to participate in the Aionima HIVE network, discover peers, and accept visitors from federated nodes.

```json
{
  "federation": {
    "enabled": false,
    "publicUrl": "https://aionima.example.com",
    "seedPeers": ["https://id.aionima.ai"],
    "autoGeid": true,
    "allowVisitors": true
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `enabled` | boolean | `false` | Enable federation protocol |
| `publicUrl` | string | — | Public URL for this node (used in manifests and peer discovery) |
| `seedPeers` | string[] | `["https://id.aionima.ai"]` | Seed peers to connect to on startup |
| `autoGeid` | boolean | `true` | Auto-generate GEID for new entities |
| `allowVisitors` | boolean | `true` | Allow visitor authentication from federated nodes |

### marketplace

Plugin marketplace configuration. Controls where the official marketplace repo is checked out and which branch is tracked.

```json
{
  "marketplace": {
    "dir": "/opt/agi-marketplace",
    "source": "git@github.com:Civicognita/agi-marketplace.git",
    "branch": "main"
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `dir` | string | `"/opt/agi-marketplace"` | Path to the official marketplace directory |
| `source` | string | `"git@github.com:Civicognita/agi-marketplace.git"` | Git remote URL |
| `branch` | string | `"main"` | Branch to track |

### mappMarketplace

MApp marketplace configuration. Controls where the official MApp marketplace repo is checked out and which branch is tracked.

```json
{
  "mappMarketplace": {
    "dir": "/opt/agi-mapp-marketplace",
    "source": "git@github.com:Civicognita/agi-mapp-marketplace.git",
    "branch": "main"
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `dir` | string | `"/opt/agi-mapp-marketplace"` | Path to the official MApp marketplace directory |
| `source` | string | `"git@github.com:Civicognita/agi-mapp-marketplace.git"` | Git remote URL |
| `branch` | string | `"main"` | Branch to track |

### dev

Developer mode configuration. When enabled, AGI switches all core repos to owner forks, allowing development work to proceed against personal fork branches rather than the upstream repos.

```json
{
  "dev": {
    "enabled": false,
    "agiRepo": "git@github.com:wishborn/agi.git",
    "primeRepo": "git@github.com:wishborn/aionima.git",
    "primeDir": "/opt/agi-prime_dev",
    "marketplaceRepo": "git@github.com:wishborn/agi-marketplace.git",
    "marketplaceDir": "/opt/agi-marketplace_dev",
    "mappMarketplaceRepo": "git@github.com:wishborn/agi-mapp-marketplace.git",
    "mappMarketplaceDir": "/opt/agi-mapp-marketplace_dev"
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `enabled` | boolean | `false` | Enable dev mode (switches core repos to owner forks) |
| `agiRepo` | string | — | Git remote URL for AGI repo fork |
| `primeRepo` | string | — | Git remote URL for PRIME repo fork |
| `primeDir` | string | `"/opt/agi-prime_dev"` | Dev directory for PRIME fork |
| `marketplaceRepo` | string | — | Git remote URL for marketplace fork |
| `marketplaceDir` | string | `"/opt/agi-marketplace_dev"` | Dev directory for marketplace fork |
| `mappMarketplaceRepo` | string | — | Git remote URL for MApp marketplace fork |
| `mappMarketplaceDir` | string | `"/opt/agi-mapp-marketplace_dev"` | Dev directory for MApp marketplace fork |

Dev mode is toggled from the dashboard at Settings > Gateway > Contributing. Enabling it clones forks into the workspace as projects with the restricted `"aionima"` project type.

### services

Per-service runtime overrides. Allows individual services to be enabled/disabled, have their port overridden, or receive extra environment variables without changing the service definition.

```json
{
  "services": {
    "overrides": {
      "my-service": {
        "enabled": true,
        "port": 4100,
        "env": {
          "LOG_LEVEL": "debug"
        }
      }
    }
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `overrides` | object | — | Per-service overrides keyed by service ID |
| `overrides[id].enabled` | boolean | — | Override whether this service is enabled |
| `overrides[id].port` | number (≥1024) | — | Override the service HTTP port |
| `overrides[id].env` | object | — | Extra environment variables injected into the service process |

### providers

System-level LLM provider credentials. Keyed by provider name. These credentials are used as fallbacks when no API key is found in the environment or the `agent` config.

```json
{
  "providers": {
    "anthropic": {
      "apiKey": "$ENV{ANTHROPIC_API_KEY}",
      "model": "claude-sonnet-4-6"
    },
    "openai": {
      "apiKey": "$ENV{OPENAI_API_KEY}",
      "baseUrl": "https://api.openai.com/v1"
    }
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `[provider].apiKey` | string | — | API key for this provider |
| `[provider].baseUrl` | string | — | Base URL override (for proxies or self-hosted endpoints) |
| `[provider].model` | string | — | Default model for this provider |

### compliance

Compliance and security controls. Enables field-level encryption for PII, MFA enforcement, and related controls required for regulated environments.

```json
{
  "compliance": {
    "encryptionAtRest": false,
    "encryptionKey": "$ENV{ENCRYPTION_KEY}",
    "requireMfa": false
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `encryptionAtRest` | boolean | `false` | Enable field-level encryption for PII at rest |
| `encryptionKey` | string | — | Hex-encoded 32-byte encryption key (or `$ENV{}` reference) |
| `requireMfa` | boolean | `false` | Require MFA for dashboard access |

Never store the raw `encryptionKey` value in `gateway.json`. Use `$ENV{ENCRYPTION_KEY}` and put the key in `.env`.

### persona

Agent soul and identity file paths. These Markdown files are loaded at boot and injected into the system prompt to shape the agent's character and purpose.

```json
{
  "persona": {
    "soulPath": "./data/persona/SOUL.md",
    "identityPath": "./data/persona/IDENTITY.md"
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `soulPath` | string | `"./data/persona/SOUL.md"` | Path to the agent soul/purpose definition |
| `identityPath` | string | `"./data/persona/IDENTITY.md"` | Path to the agent identity definition |

### agentCredentials

Credentials the agent uses when acting on the owner's behalf in external services such as email and GitHub.

```json
{
  "agentCredentials": {
    "email": {
      "provider": "google",
      "address": "agent@example.com"
    },
    "github": {
      "username": "my-agent-bot"
    }
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `email.provider` | enum | — | Email provider: `"google"` or `"outlook"` |
| `email.address` | string | — | Email address the agent sends from |
| `github.username` | string | — | GitHub username the agent acts as |

### backup

Automated backup configuration. When enabled, AGI periodically snapshots runtime data (`~/.agi/`) and retains backups for the configured number of days.

```json
{
  "backup": {
    "enabled": true,
    "dir": "~/.agi/backups",
    "retentionDays": 30
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `enabled` | boolean | `true` | Enable automated backups |
| `dir` | string | `"~/.agi/backups"` | Backup directory path |
| `retentionDays` | number | `30` | Days to retain backups before pruning |

### identity

OAuth provider credentials for local identity issuance. Used when the local ID service is hosting sign-in flows directly, rather than delegating to the remote HIVE identity service.

```json
{
  "identity": {
    "oauth": {
      "google": {
        "clientId": "$ENV{GOOGLE_CLIENT_ID}",
        "clientSecret": "$ENV{GOOGLE_CLIENT_SECRET}",
        "scopes": ["openid", "email", "profile"]
      },
      "github": {
        "clientId": "$ENV{GITHUB_CLIENT_ID}",
        "clientSecret": "$ENV{GITHUB_CLIENT_SECRET}"
      }
    }
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `oauth.google.clientId` | string | required | Google OAuth client ID |
| `oauth.google.clientSecret` | string | required | Google OAuth client secret |
| `oauth.google.scopes` | string[] | — | OAuth scopes to request |
| `oauth.github.clientId` | string | required | GitHub OAuth client ID |
| `oauth.github.clientSecret` | string | required | GitHub OAuth client secret |
| `oauth.github.scopes` | string[] | — | OAuth scopes to request |

Store client secrets via `$ENV{}` references — never paste them directly into `gateway.json`.

### chat

Chat history configuration. Controls how long chat sessions are retained before they are garbage collected.

```json
{
  "chat": {
    "retentionDays": 30
  }
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `retentionDays` | number | `30` | Days to retain chat sessions before garbage collection |

---

## Hot-Reload

The gateway watches `gateway.json` for changes using a file watcher. When the file changes:

1. The new config is loaded and validated against the Zod schema.
2. If valid, the `config:changed` hook is fired for each changed key.
3. Plugins and internal components that registered `config:changed` handlers update their state.
4. Channel adapters that support hot-reload pick up new settings without restarting.

Not all config changes hot-reload cleanly. Changes to `gateway.host`, `gateway.port`, and `entities.path` require a full gateway restart.

Changes to channel credentials (bot tokens, API keys) require the affected channel to be restarted. Use the dashboard channel controls to stop and start the channel after updating credentials.

---

## Example: Minimal Config

```json
{
  "gateway": {
    "host": "127.0.0.1",
    "port": 3100,
    "state": "ONLINE"
  },
  "auth": {
    "tokens": ["$ENV{AUTH_TOKEN}"]
  },
  "agent": {
    "provider": "anthropic",
    "model": "claude-sonnet-4-6",
    "replyMode": "autonomous"
  },
  "owner": {
    "displayName": "Owner",
    "dmPolicy": "pairing",
    "channels": {}
  },
  "channels": [],
  "entities": {
    "path": "./data/entities.db"
  },
  "workspace": {
    "root": "."
  },
  "logging": {
    "level": "info"
  }
}
```

This is the content of `aionima.example.json` — the starting point for a new installation. Run `aionima setup` to generate a populated version with production paths (e.g., `~/.agi/entities.db` instead of `./data/entities.db`).
